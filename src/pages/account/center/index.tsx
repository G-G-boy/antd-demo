import {FC} from 'react';
import PageContainer from '@/components/page-container';

const AccountCenter: FC = () => {
    return <PageContainer>个人中心</PageContainer>;
};

export default AccountCenter;
